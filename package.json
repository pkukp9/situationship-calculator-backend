{
  "name": "situationship-calculator",
  "version": "1.0.0",
  "description": "API for analyzing situationships using GPT-4",
  "main": "api/analyze-text.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "openai": "^4.28.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
